# Backend Tests

Placeholder for backend tests. Add your preferred test runner
(pytest, unittest, httpx TestClient, etc.) and put test files in this folder.

Example (pytest + httpx):

```
pip install pytest httpx
pytest
```
