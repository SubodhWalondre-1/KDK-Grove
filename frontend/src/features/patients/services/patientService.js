import { getSpecies, getProfiles, createProfile } from '../../../services/api/patients';

export const patientService = {
  getSpecies,
  getProfiles,
  createProfile,
};

export default patientService;
